language: objective-c
osx_image: xcode9.3

before_install:
  - pod repo update

pods: remove_debug_pods
pods:
  rm -rf Pods
  bundle install
  bundle exec pod install

xcode_project: Tsukuba-iOS.xcworkspace
xcode_scheme: Tsukuba-iOS
